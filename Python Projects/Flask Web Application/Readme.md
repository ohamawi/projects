This is my first attempt at creating a web application!

Using the Flask library for Python, I'm going to be combining a couple of my projects into one, accessible web application!

Combining the Finance Tracker, Scheduler and potentially a Journal on top of it.

Current Implementations in progress:

Finance Tracker:

  The ability to create and edit transactions and incomes
  The ability to choose the dates of a transaction instead of the current date being automatically the only one available
  The ability to save unique entries for the user, which would allow several users on the platform at once
